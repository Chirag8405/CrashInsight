# Core dependencies
Flask==2.3.2
flask-cors==4.0.0
gunicorn==20.1.0

# Data processing (using older stable versions)
pandas==1.5.3
numpy==1.23.5

# Machine Learning (older stable versions)
scikit-learn==1.2.2

# Optional ML extensions (will add back after core works)
# mlxtend==0.22.0
# graphviz==0.20.1